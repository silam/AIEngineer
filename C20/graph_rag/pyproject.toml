[project]
name = "graph-rag"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Si Lam", email = "silam@hotmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "langchain-experimental>=0.4.2",
    "matplotlib>=3.11.1",
    "python-dotenv>=1.2.2",
]

[project.scripts]
graph-rag = "graph_rag:main"

[build-system]
requires = ["uv_build>=0.12.10,<0.13.0"]
build-backend = "uv_build"
