[project]
name = "corrective-rag"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Si Lam", email = "silam@hotmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "langchain>=1.3.14",
    "langchain-community>=0.4.2",
    "langgraph>=1.2.9",
    "python-dotenv>=1.2.2",
]

[project.scripts]
corrective-rag = "corrective_rag:main"

[build-system]
requires = ["uv_build>=0.12.10,<0.13.0"]
build-backend = "uv_build"
